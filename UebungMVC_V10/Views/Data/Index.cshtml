@model IEnumerable<UebungMVC_V10.Models.VModels.DataVM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AnfangDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndeDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AbstandDateTimeText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AnfangDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndeDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AbstandDateTimeText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DatumID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DatumID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DatumID })
        </td>
    </tr>
}

</table>
